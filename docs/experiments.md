# 측정 규칙과 결과

규칙은 재기 전에 적고 커밋한다. 결과는 같은 절 아래에 덧붙인다. 규칙을 바꾸면 바꾼 시점과 이유를 남긴다.

## 공통 규칙 (2026-09-19, 측정 전)

### 판정
- 성공 = (1) 에피소드가 끝난 DB 덤프가 "새 복사본에 정답 동작만 실행한 덤프"와 같고 (2) 꼭 전달해야 하는 값이 고객에게 전달된 상담원 발화에 있다. LLM 판정은 쓰지 않는다. 원 τ-bench 논문의 방식(행동 결과 × 전달 정보)이다
- 비교에서 빼는 컬럼은 `db.IGNORED_COLUMNS`뿐이다: 티켓 본문, 이관 요약, 이관 사유, 이관 행의 고객 번호 (규정이 본인 확인 전후 어느 쪽의 이관도 허용하므로)
- 성공 조건에 "정답에 없는 쓰기가 0건"은 넣지 않는다 (τ-bench와 같다. 바꿨다가 되돌린 쓰기는 DB가 같으면 성공이다). 대신 성공했지만 정답에 없는 쓰기가 있었던 건수를 함께 보고한다
- 전달 값 검사: 숫자는 숫자 사이의 쉼표만 지운 뒤 앞뒤가 숫자·영문자·`-`·`.`이 아닌 위치에서 찾는다. 날짜는 월·일이 같고 연도가 없거나 같으면 일치. 글자는 NFKC·소문자·공백 제거 뒤 부분 문자열. 한글 수 표기("3만 2천 원")는 인식하지 않는다
- 고객의 종료 토큰은 `### stop ###`처럼 조금 다르게 써도 인정한다. 토큰과 함께 온 말("네, 진행해 주세요 ###STOP###")은 버리지 않고 에이전트에게 전달해 한 번 답하게 한 뒤 끝낸다. 시뮬레이터의 버릇이 에이전트의 실패로 세어지지 않게 하기 위해서다
- 판정하는 종료는 고객의 종료(`user_stop`), 시나리오 밖(`out_of_scope`), 이관(`handoff`), 고객 턴 한도(`max_user_turns`)다. 에이전트 쪽 한도 초과(LLM 호출, 도구 오류, 컨텍스트)와 형식 오류 종료는 실패로 센다. 고객 턴 한도는 2026-09-19에 판정 대상으로 옮겼다: 시뮬레이터를 고친 뒤 기준선에서 일이 끝났는데도 시뮬레이터가 종료 토큰을 내지 못해 20턴을 채운 에피소드가 3개 있었다. 대화를 끝내는 것은 시뮬레이터의 몫이고, 에이전트가 시간을 끌어 얻는 것은 없다. 분석 도구는 기록된 판정 요소(DB 일치, 전달 값, 종료 사유)로 성공을 다시 계산한다 제공자 오류, 시뮬레이터의 빈 응답, 도구 코드의 버그는 `infra_error`로 기록하고 분모에서 뺀다. 공식 실행에서 `infra_error`가 나오면 건수를 결과에 적는다

### 에이전트 루프
- 텍스트와 도구 호출이 함께 오면 도구 호출을 따르고 텍스트는 고객에게 보내지 않는다 (τ-bench v1과 같은 관대한 쪽)
- 도구 호출이 여러 개면 첫 번째만 실행한다
- 형식 오류(빈 응답, 본문에 샌 도구 호출, 길이 제한으로 잘림)는 고객에게 보내지 않고, 하니스 안내문을 넣어 한 턴에 2번까지 다시 시킨다. 넘으면 실패. 본문에 샌 도구 호출은 찾아내기만 하고(태그, 또는 본문 속 `name`+`arguments` JSON) 구제 실행하지 않는다. 형식 오류 수와 재시도 수를 결과와 함께 보고한다
- 한도: 에이전트 LLM 호출 30, 고객 턴 20, 도구 오류 10. 프롬프트가 `num_ctx`의 95%를 넘으면 종료
- 본인 확인은 실험 축이 아니다. 모든 조건에서 도구가 막고, 막힌 횟수를 센다

### 도구와 규정
- 읽기 도구는 사실만 돌려준다. "반품 가능 여부"나 "반품 기한" 같은 계산된 판단은 주지 않는다 (주면 P0와 P1의 차이를 도구 출력이 메운다)
- 도구 오류 메시지에는 이유만 적고 다음 행동 힌트를 넣지 않는다
- P0와 P1의 시스템 프롬프트와 규정 문서는 같다. 다른 것은 도구의 규정 검사 동작뿐이다
- 정답 재실행과 과제 검증은 항상 P1로 돌린다

### 과제
- 과제는 `seed.py`의 고정 행만 참조한다. 과제의 `now`는 seed의 모든 시각보다 뒤다 (쿠폰 만료 시각은 예외)
- 쓰기 인자를 결정하는 모든 선택(사유, 대상 줄, 교환할 옵션, 주소)은 시나리오의 `known`에 적는다. 시뮬레이터가 고를 여지를 두지 않는다
- 전달 값은 도구 결과에서만 알 수 있는 값으로 하고, 시나리오와 규정 문서에 나오는 값("7일" 등)이나 한두 자리 수는 쓰지 않는다. 과제 검증이 시나리오와 에이전트 시스템 프롬프트 전체를 검사한다 (2026-09-19 검토에서 규정 문서의 예시 날짜가 거절 과제의 전달 값과 같아, 조회 없이 예시만 말해도 통과하는 것을 찾아 고쳤다)
- 쓰기 대상 주문은 할인이 0원인 주문으로 한다 (반품 환불액의 할인 안분 규칙은 아직 없다)
- 여러 줄 반품은 한 번에 접수하는 것이 규정이다 (나눠 접수하면 반품 배송비가 두 번 빠져 DB가 달라진다)
- 한 과제에서 같은 부모 아래 같은 테이블에 새로 생기는 행은 1개까지
- 시뮬레이터는 시나리오에 없는 제안(쿠폰, 문의 접수, 반품·교환, 상담원 연결)을 거절하고, 처리를 부탁한 과제에서는 처리가 끝났다는 말을 들은 뒤에만 대화를 끝낸다 (시뮬레이터 지침의 공통 규칙). 2026-09-19 과제 검토에서, 확인 질문에 값이 미리 나와 마지막 쓰기 전에 고객이 대화를 끝낼 수 있는 과제와 요청하지 않은 쿠폰 제안을 고객이 받아들일 수 있는 과제를 찾아 넣었다
- 측정을 시작한 과제는 고치지 않는다. 고치면 시점과 이유를 적고 그 과제를 다시 잰다
- 개발용 과제로 프롬프트와 가드를 고치고, 시험용 과제는 단계가 끝날 때 고른 후보만 한 번 잰다. 시험용 에피소드를 보면서 고치지 않는다

### 무작위성
- 에이전트 temperature 0.0, 시뮬레이터 0.3 (2026-09-19 기준선 첫 실행 뒤 0.7에서 낮춤. 아래 1단계 결과 참조). LLM 호출마다 seed = sha256(기준 seed, 과제, 시도, 역할, 호출 번호). temperature 0에서는 seed가 의미가 없고, 시도마다 달라지는 것은 시뮬레이터의 말이다
- GPU 연산이라 비트 단위 재현은 약속하지 않는다

## 0단계: 스모크 (측정 아님)

`tasks/smoke.yaml`의 과제 5개를 실제 모델로 한 번씩 돌려 에피소드가 끝까지 돌고 판정이 나오는지, 에피소드당 시간이 얼마인지 본다. 수치를 결과로 쓰지 않는다. 이때 확인해 1단계 규칙에 반영할 것:

- 시스템 프롬프트(규정 + 도구)의 실제 토큰 수와 `num_ctx` 16384의 여유
- `prompt_eval_count`가 캐시된 토큰을 포함하는지 (컨텍스트 한도 감지가 의존)
- 에이전트와 시뮬레이터가 같은 모델을 번갈아 쓸 때의 프롬프트 재평가 시간
- 모델이 enum 코드 인자를 제대로 쓰는지, 형식 오류가 얼마나 나는지

### 결과 (2026-09-19, `qwen2.5:7b-instruct`, P0·R0, RTX 2080 Ti, Ollama 0.34.1. 측정 아님)

두 번 돌렸다. 첫 실행 뒤 기준 프롬프트의 기본기를 고쳤고(아래), 그 뒤 다시 돌렸다. 과제 5개를 한 번씩이라 성공 수는 결과로 쓰지 않는다.

| | 첫 실행 (`8450dcc`) | 프롬프트 수정 뒤 |
|---|---|---|
| 성공 | 1/5 | 3/5 |
| 형식 오류 | 7 | 0 |
| 에피소드당 시간 | 56.8초 | 19.4초 |
| 에이전트 LLM 호출당 시간 | 1.9초 | - |

확인한 것
- 에피소드가 끝까지 돌고 판정이 나온다. `infra_error` 0건
- 시스템 프롬프트(규정 + 도구 13개)는 약 3,400토큰이고 대화가 끝날 때 최대 5,160토큰이었다. `num_ctx` 16384에 여유가 있다
- `prompt_eval_count`는 캐시된 토큰을 포함한 전체 프롬프트 토큰 수다 (첫 호출 3,439토큰에 4.3초, 그 뒤로는 토큰 수가 계속 늘면서 프롬프트 평가는 0.04~0.16초). 컨텍스트 한도 검사가 이 값에 기대도 된다
- 에이전트와 시뮬레이터가 같은 모델을 번갈아 불러도 프롬프트 캐시가 유지되고 모델이 다시 로드되지 않는다. 처음 모델을 올리는 데 92초(HDD)
- 도구 호출 id가 온다(13/13). enum 코드 인자(`ordered_by_mistake` 등)는 제대로 쓴다
- P0에서 기한이 지난 반품을 모델이 그대로 접수했고 `return_window_expired` 위반으로 기록됐다. P0와 P1을 가를 여지가 있다

첫 실행에서 본 실패 (하니스가 아니라 모델 행동)
- 고객 이름을 로마자(" PARK Do-yun")로 바꿔 `find_customer`에 넣어 본인 확인에 세 번 실패
- "확인해 보겠습니다, 잠시만 기다려 주세요"라고만 하고 도구를 부르지 않음. 도구를 부르지 않고 "취소가 완료되었습니다"라고 말함
- `<tool_call>` 여는 태그가 깨져(`portun`) 도구 호출이 본문으로 샘: 한 에피소드에서 7번. 규칙대로 구제 실행하지 않았고 그 에피소드는 형식 오류로 끝남
- 시뮬레이터가 중국어로 말하거나, 상담원의 "기다려 주세요"를 따라 말함

그래서 기준 프롬프트에 넣은 것 (측정 전, 스모크 과제만 보고 고침)
- 에이전트: 기다리라는 말 대신 바로 도구를 호출한다 / 도구 성공 결과를 받기 전에는 처리되었다고 말하지 않는다 / 이름과 주문 번호는 고객이 말한 글자 그대로 넣는다 / 항상 한국어로 답한다
- 시뮬레이터: 항상 한국어로만 말한다 / 기다려 달라고 하면 "네"라고만 답한다

2단계 후보로 남긴 것: 본문에 샌 도구 호출을 구제 실행하는 축(F1). 기준선은 구제하지 않는다.

## 1단계: 과제와 기준선 (규칙 2026-09-19, 측정 전)

### 과제 묶음
- `tasks/dev.yaml` 24개: 조회 6, 처리 8, 거절 6, 복합 4
- `tasks/test.yaml` 40개: 조회 8, 처리 14, 거절 10, 복합 8
- 두 묶음은 고객과 주문을 공유하지 않는다 (개발용 `C-91xx`/`O-91xxx`, 시험용 `C-92xx`/`O-92xxx`). 같은 종류의 규정을 다루되 금액, 날짜, 상품, 말투가 다르다
- 쓰기 도구(취소, 반품, 교환, 배송지 변경, 보상 쿠폰, 티켓, 이관)는 두 묶음 모두에 고르게 나온다. 규정 검사 코드는 시험용이 더 넓다: `return_not_delivered`, `coupon_not_eligible`, `coupon_limit_exceeded`, `coupon_order_cancelled`는 시험용에만 있어, 이 네 코드의 실패는 개발용으로 분석할 수 없다 (한계). 거절 과제에는 기한 하루 전후 같은 경계 사례를 넣는다 (기한 마지막 날은 처리, 다음 날은 거절)
- 개발용으로 프롬프트와 가드를 고친다. 시험용은 단계가 끝날 때 고른 구성만 한 번 재고, 시험용 에피소드를 보면서 무언가를 고치지 않는다
- 한계: 시험용 과제 몇 개는 개발용 과제와 구조가 닮았다 (같은 사유 코드나 같은 종류의 경계 사례. 고객, 주문, 금액은 다르다). 2026-09-19 검토에서 7쌍을 찾았고, 측정 전이지만 과제를 다시 쓰지는 않았다

### 기준선
- 구성: P0 · R0 · `qwen2.5:7b-instruct`, 시뮬레이터도 같은 모델. 과제마다 4번
- 재는 것: pass^1~pass^4(과제 단위 부트스트랩 95% 구간), 유형별 성공률, 정답에 없는 쓰기 수, P0에서 통과된 규정 위반 코드별 횟수, 본인 확인 전 접근이 막힌 횟수, 형식 오류 수, 종료 사유별 건수, 에피소드당 시간
- 판정은 없다. 뒤 단계의 비교 기준이 된다
- 실패 분류는 개발용 에피소드로만 한다: 본인 확인 누락, 규정 위반, 잘못된 인자, 고객 확인 없이 실행, 값 전달 누락, 중간에 멈춤, 시뮬레이터 탓
- 시뮬레이터 점검: 개발용 에피소드 20개를 무작위(고정 seed)로 뽑아 읽고, 시나리오에 없는 정보를 지어냄 / 일찍 포기함 / 상담원 역할을 함 / 틀린 확인 요청에 동의함을 센다

### 결과 1: 첫 기준선과 시뮬레이터 점검 (2026-09-19, 커밋 `f4ec9a4`, RTX 2080 Ti) — 시뮬레이터를 고치게 되어 기준선으로 쓰지 않는다

`reports/20260919-093328-qwen2.5-7b-instruct-P0-R0-baseline` (96 에피소드, infra 0, 중단 20, 에피소드당 22.9초)

| pass^1 | pass^2 | pass^3 | pass^4 |
|---|---|---|---|
| 22.9% [11.5%, 35.4%] | 11.8% [2.8%, 22.9%] | 7.3% [1.0%, 16.7%] | 4.2% [0.0%, 12.5%] |

- 값을 말하지 않아 실패한 20개 에피소드(값 23개)를 느슨한 검색(숫자만 이어 붙여 찾기, 월과 일이 따로 있는지)으로 다시 봤다. 23개 모두 상담원 발화에 정말 없었다. 판정기의 거짓 실패는 찾지 못했다
- **시뮬레이터 점검** (`analyze sample --n 20`, 고정 seed로 뽑은 20개를 읽음)

| 시뮬레이터의 잘못 | 에피소드 수 (20개 중) |
|---|---|
| 시나리오에 없는 사실을 지어냄 (전화번호 010-1234-5678, 회사 주소, 수령일, 가격. "전화번호는 알려 드리기 꺼려진다"며 임시 비밀번호를 요구) | 5 |
| 값을 듣기 전에 대화를 끝냄 | 2 |
| 상담원처럼 말함 ("표준영님, 안녕하세요", "추가 도움이 필요하시면 연락 주세요") | 3 |
| 상담원에게 "기다려 주세요"라고 함 | 3 |
| 이관과 무관한 과제에서 "네, 연결해 주세요" (같은 날 시뮬레이터 지침에 넣은 문장의 부작용) | 6 |
| 다른 언어가 섞임 | 1 |
| 결과를 바꿨을 만한 잘못이 하나라도 있음 | 7 |

  종료 토큰 대신 인사로 대화를 이어 가는 경우도 많았다. τ²-bench 논문이 GPT-4.1 시뮬레이터에 대해 보고한 오류율(retail 40%, 치명 12%)보다 나쁘다. 이 상태로는 에이전트의 실패와 시뮬레이터의 실패를 가를 수 없다고 판단했다

**시뮬레이터를 바꿨다 (2026-09-19, 개발용 에피소드만 보고).** 규칙대로 이 단계의 측정을 모두 다시 한다.
- 매 턴, 모델에 보내는 마지막 메시지 끝에 짧은 역할 지침과 "당신이 아는 정보"(시나리오의 `known`)를 붙인다. 기록에는 붙이지 않는다. 7B는 몇 턴 지나면 시스템 프롬프트의 규칙을 잊는다
- temperature 0.7 → 0.3
- 지침: 시나리오에 없는 정보를 요구받으면 모른다고 하고 아는 정보를 주며 계속한다. `###OUT-OF-SCOPE###`는 같은 막힘이 세 번 넘게 되풀이될 때만. 인사로 대화를 끝내지 않는다. "네, 연결해 주세요" 문장은 공통 지침에서 빼고 이관 과제의 `rules`에만 둔다
- 고치는 동안 본 것: 리마인더에 "끝내는 조건이 채워졌으면 STOP"을 넣으면 첫 턴에 바로 STOP을 낸다. 그래서 "원하던 일이 모두 끝난 뒤에는"으로 썼다
- 함께 고친 것: 모델이 한 토큰을 되풀이하다 Ollama가 HTTP 500("token repeat limit reached")으로 끊는 경우를 인프라 오류가 아니라 빈 응답(형식 오류)으로 센다. 에이전트면 형식 오류 재시도, 시뮬레이터면 `infra_error`

### 결과 2: 기준선 (2026-09-19, 커밋 `e86aa05`의 측정 코드, 시뮬레이터 수정 뒤, RTX 2080 Ti)

`reports/20260919-102552-qwen2.5-7b-instruct-P0-R0-baseline2` — P0 · R0 · `qwen2.5:7b-instruct`, 개발용 24과제 × 4회 = 96 에피소드, `infra_error` 0, 에피소드당 18.3초. 표는 `uv run python -m support_agent.analyze table <run>`으로 다시 나온다 (고객 턴 한도를 판정 대상으로 옮긴 규칙으로 계산. 위 "판정" 참조)

| | pass^1 | pass^2 | pass^3 | pass^4 |
|---|---|---|---|---|
| 기준선 | 20.8% [9.4%, 34.4%] | 12.5% [2.8%, 25.0%] | 9.4% [0.0%, 21.9%] | 8.3% [0.0%, 20.8%] |
| (참고) 결과 1을 같은 규칙으로 다시 계산 | 24.0% [11.5%, 37.5%] | 13.2% | 8.3% | 4.2% |

| 유형 | 과제 | pass^1 |
|---|---|---|
| 조회 | 6 | 41.7% [8.3%, 75.0%] |
| 처리 | 8 | 28.1% [12.5%, 46.9%] |
| 거절 | 6 | 4.2% [0.0%, 12.5%] |
| 복합 | 4 | 0.0% [0.0%, 0.0%] |

- 한 번 성공률과 네 번 모두 성공할 비율의 차이가 크다 (20.8% → 8.3%). 네 번 모두 성공한 과제는 24개 중 2개(조회 과제 dev-001, dev-003)뿐이다
- 정답에 없는 쓰기 11건, P0에서 통과된 규정 위반 8건(`exchange_different_product` 3, `return_window_expired` 2, `exchange_window_expired` 1, `address_change_not_allowed_status` 1, `coupon_not_eligible` 1), 본인 확인 전 접근 차단 34건, 형식 오류 12건
- 시뮬레이터를 고친 뒤 성공률이 오르지 않았다. 고치기 전의 시뮬레이터는 상담원이 말하지 않은 값을 "들었다"고 하거나 틀린 안내를 받아들여 대화를 끝내 주는 일이 있었고, 고친 뒤에는 끝까지 요구해서 에이전트의 멈춤이 그대로 드러난다

**실패 분류** (개발용 96 에피소드, 기록에서 자동 분류한 뒤 대화를 읽어 확인)

| 분류 | 에피소드 | 설명 |
|---|---|---|
| 성공 | 20 | |
| 멈춤: 일을 끝내지 못하고 고객 턴 한도(20)에 닿음 | 31 | "확인해 보겠습니다", "환불이 완료되면 알려 드리겠습니다"를 되풀이하고 도구를 부르지 않는다. temperature 0이라 같은 말이 그대로 반복된다 |
| 쓰기를 하지 않고 끝남 | 19 | 도구를 부르지 않고 "접수되었습니다"라고 말하거나(거짓 완료), 확인 질문만 하고 끝난다 |
| DB는 맞는데 값을 말하지 않음 | 15 | 주소를 "개인 정보 보호"라며 말하지 않음, 출고일·수령일을 묻는데 상태만 되풀이, 없는 주소를 지어냄 |
| 잘못된 쓰기 | 8 | 그중 5건이 규정 위반(기한 지난 반품·교환, 다른 상품으로 교환) |
| 형식 오류·도구 오류 누적으로 종료 | 3 | |

- 가장 큰 실패는 규정 위반이 아니라 **행동하지 않는 것**(멈춤 + 쓰기 없음 = 50건)이다. 2단계 후보에 이것을 겨냥한 축을 넣는다
- 복합 과제는 16번 중 한 번도 성공하지 못했다

## 2단계: 개선 실험 (규칙 2026-09-21, 측정 전)

### 공통 설정
- 기준은 1단계의 기준선(P0 · R0 · G0 · F0 · `qwen2.5:7b-instruct`, `reports/20260919-102552-...-baseline2`)이다. 과제, 프롬프트, 규정 문서, 시뮬레이터(모델·프롬프트·temperature 0.3)는 기준선과 같다. 후보마다 개발용 24과제 × 4회
- 시뮬레이터 모델은 모든 후보에서 `qwen2.5:7b-instruct`로 고정한다. 에이전트 모델이 다른 후보(M1)에서는 두 모델이 GPU에 함께 올라가지 못하면 턴마다 모델을 다시 읽어 느려진다. 그 경우 시간 수치는 비교에 쓰지 않는다

### 후보 (기준에서 한 축씩만 바꾼다)

| 후보 | 바꾸는 것 | 근거 (1단계 실패 분류) |
|---|---|---|
| P1 | 코드로 검증할 수 있는 규정을 도구 안에서도 막는다 | 잘못된 쓰기 8건 중 5건이 규정 위반. 거절 과제 pass^1 4.2% |
| R1 | think 도구를 보여 준다 | 복합 과제 0/16, 값을 말하지 않음 15건 |
| G1 | 멈춤 가드: 도구 호출 없이 "확인해 보겠습니다 / 잠시만 기다려 주세요"처럼 하겠다는 말만 하고 고객에게 묻는 것도 없는 응답은 고객에게 보내지 않고, 하니스 안내문을 넣어 다시 시킨다. 한 턴에 2번까지, 넘으면 그대로 전달 | 멈춤 31건 + 쓰기 없이 끝남 19건 = 96건 중 50건 |
| F1 | 본문에 샌 도구 호출을 찾아 실행한다 (형식 오류로는 그대로 센다) | 형식 오류 12건, 형식 오류 종료 2건. 효과는 작을 것으로 예상 |
| M1 | 에이전트 모델을 `qwen2.5:14b-instruct`로 | 계획의 질문 5 |

- G1의 "하겠다는 말"은 정규식으로 정한다 (`agent.is_stall`): 확인·조회·처리·접수·진행·발급·변경·취소·연결·검토 + "하겠습니다/해 보겠습니다/해 드리겠습니다", "잠시만", "기다려 주". 물음표나 "알려 주/말씀해 주"가 있으면 멈춤이 아니다. 개발용 기준선 에피소드를 보고 정했고 시험용은 보지 않았다
- 새 세대 모델(qwen3 등)과 한국어 특화 모델은 이번 단계에 넣지 않는다 (thinking 옵션 처리와 내려받기가 필요. 뒤로 미룸)

### 선택 규칙
1. 후보마다 개발용 pass^1의 기준과의 차이를 과제 단위 짝지은 부트스트랩(10,000회, 고정 seed)으로 낸다: `analyze compare`
2. 차이가 +5%p 이상이고 정답에 없는 쓰기 수가 기준보다 늘지 않았으면 그 축을 **채택**한다. 95% 구간이 0을 포함하는지는 함께 적되 채택 조건으로 쓰지 않는다 (24과제로는 검정력이 작다. 판정은 시험용으로 한다)
3. M1은 위 조건에 더해 에피소드당 시간이 기준의 3배 이하일 때만 채택한다
4. 채택한 축을 모두 합친 구성(**최종 구성**)을 개발용으로 한 번 더 잰다. 합친 구성의 pass^1이 채택한 단일 후보 중 가장 높은 것보다 5%p 넘게 낮으면 그 단일 후보를 최종 구성으로 한다
5. 채택한 축이 하나도 없으면 시험용은 기준만 재고 "개선 없음"으로 적는다

### 판정 규칙 (시험용 40과제 × 4회, 기준과 최종 구성을 각각 한 번)
1. pass^1의 차이(최종 − 기준)의 95% 구간이 0보다 위에 있다
2. 정답에 없는 쓰기 수가 기준보다 많지 않다

| 1 | 2 | 판정 |
|---|---|---|
| 만족 | 만족 | **개선** |
| 만족 | 불만족 | **성공률은 올랐지만 위험한 쓰기가 늘었음** |
| 불만족 | - | **개선을 확인하지 못함** |

pass^4의 차이와 유형별 성공률, 통과된 규정 위반 수는 함께 보고하되 판정에는 넣지 않는다.

### test
`uv run python -m support_agent.run --tasks test --trials 4 --allow-test --official` 을 기준과 최종 구성으로 한 번씩. 시험용 에피소드는 결과를 적은 뒤에도 프롬프트나 가드를 고치는 데 쓰지 않는다.

### M1을 재기 전에 정한 것 (2026-09-21, 과제 2개로 시간만 확인한 뒤)
- 14B(9GB)와 시뮬레이터 7B는 11GB GPU에 함께 올라가지 못한다. 시뮬레이터를 CPU에서 돌린다 (`--user-on-cpu`. 모델, 프롬프트, temperature는 같다). 두 모델이 함께 메모리에 남아 다시 읽지 않는 것을 확인했다
- 14B는 16k 컨텍스트에서 GPU에 들어가지 않아 `--num-ctx 8192`로 잰다 (기준선의 프롬프트 최대 길이는 5,160토큰). 8192에서도 12%가 CPU로 내려간다
- 에피소드당 시간은 CPU 시뮬레이터가 대부분을 차지해(호출당 16초) 기준과 비교할 수 없다. 선택 규칙 3의 "에피소드당 시간"은 **에이전트 LLM 호출당 시간**으로 바꿔 본다 (기준의 3배 이하)

### 추가 후보 G2 (2026-09-21, P1·R1·G1·F1의 개발용 결과를 본 뒤에 추가. 재기 전에 적음)
- G1은 하겠다는 말만 하는 응답을 정확히 잡았지만(423번) 성공률은 내려갔다. 개발용 에피소드를 보면 temperature 0인 모델이 안내문을 받고도 같은 문장을 그대로 되풀이해 LLM 호출 한도만 쓴다 (`max_agent_calls` 0 → 16건, 형식 오류 종료 2 → 11건)
- G2 = G1 + 멈춤으로 돌려보낸 뒤 그 턴의 남은 호출은 temperature 0.7로 뽑는다 (seed는 호출마다 다르다). 그 밖은 G1과 같다
- 결과를 보고 만든 후보이므로 개발용 수치는 낙관적일 수 있다. 선택 규칙은 다른 후보와 같고, 판정은 시험용으로만 한다

### 선택 (2026-09-21, 개발용 24과제 × 4회, Ollama 0.34.2, RTX 2080 Ti)

기준선을 잰 뒤 Ollama가 0.34.1에서 0.34.2로 저절로 올라갔다 (manifest의 `ollama_version`으로 알았다). 그래서 기준을 같은 환경에서 다시 쟀고(`...-baseline3`), pass^1 20.8%가 그대로 나왔다. 아래 차이는 모두 이 기준과 짝지은 것이다. `uv run python -m support_agent.analyze compare reports/*baseline3 reports/*-p1 ...`

| 후보 | pass^1 | 차이 [95% 구간] | pass^4 | 정답에 없는 쓰기 | 통과된 규정 위반 | 형식 오류 | 채택 |
|---|---|---|---|---|---|---|---|
| 기준 (P0·R0·G0·F0·7B) | 20.8% | | 12.5% | 12 | 12 | 18 | |
| P1 규정을 도구에서도 막음 | 15.6% | −5.2%p [−18.8, +7.3] | 4.2% | **2** | **0** (막힘 9) | 48 | 아니오 |
| R1 think 도구 | 13.5% | −7.3%p [−19.8, +3.1] | 4.2% | 12 | 7 | 34 | 아니오 |
| G1 멈춤 가드 | 11.5% | −9.4%p [−20.8, +0.0] | 4.2% | 14 | 12 | 34 (+멈춤 423) | 아니오 |
| G2 멈춤 가드 + 재시도를 0.7로 | 11.5% | −9.4%p [−21.9, +2.1] | 4.2% | 11 | 10 | 14 | 아니오 |
| F1 샌 도구 호출 구제 | 17.7% | −3.1%p [−8.3, +1.0] | 4.2% | 16 | 14 | 2 | 아니오 |
| M1 14B (컨텍스트 8k, 시뮬레이터 CPU) | **47.9%** | **+27.1%p [+11.5, +42.7]** | 29.2% | 18 | 11 | 21 | 아니오 (아래) |

- 7B에서 시도한 다섯 후보는 모두 기준을 넘지 못했다. 차이의 구간은 모두 0을 포함하므로 "나빠졌다"고도 말할 수 없다. +5%p 조건을 만족한 것이 없어 **채택 없음**
- P1은 성공률을 올리지 못했지만 하려던 일은 했다: 정답에 없는 쓰기 12 → 2, 통과된 규정 위반 12 → 0. 대신 도구에 막힌 뒤 모델이 도구 호출을 본문에 쓰는 일이 늘었다 (형식 오류 18 → 48)
- G1·G2: 하겠다는 말만 하는 응답을 정확히 잡아도(G1에서 423번) 7B는 안내문을 받고 행동으로 옮기지 못한다. temperature를 올려도 같았다. LLM 호출 한도에 닿는 에피소드만 늘었다 (0 → 16, 20건). **멈춤은 출력 형식의 문제가 아니라 모델이 다음에 무엇을 해야 할지 모르는 문제**로 보인다
- M1은 성공률이 두 배 넘게 올랐다 (조회 37.5 → 79.2%, 처리 34.4 → 68.8%, 거절 0 → 16.7%, 복합 0 → 6.2%). 그러나 미리 정한 채택 조건 두 개에 걸린다: 에이전트 LLM 호출당 시간 0.53초 → 4.16초로 7.8배(조건은 3배 이하. 11GB GPU에 다 올라가지 않아 12%가 CPU에서 돈다), 정답에 없는 쓰기 12 → 18건. 더 자주 행동하는 만큼 규정을 어기는 쓰기도 늘었다. **규칙대로 채택하지 않는다**
- 선택 규칙 5에 따라 최종 구성은 없다. 시험용은 기준만 잰다

### 참고 측정을 하나 더한다 (2026-09-21, 시험용을 보기 전에 적음. 판정과 무관)
- M1을 시험용 40과제 × 4회로 한 번 잰다 (개발용과 같은 설정: 컨텍스트 8k, 시뮬레이터 CPU). 이유: 이 단계에서 가장 큰 차이가 모델 크기였고, 그 차이가 개발용 24과제에만 있는 것인지 보려는 것이다
- 이 수치로 아무것도 고르지 않는다. 판정표에는 넣지 않고 "참고"로만 적는다. 시험용 에피소드는 이후에도 프롬프트나 가드를 고치는 데 쓰지 않는다

### 결과 (2026-09-21, 시험용 40과제 × 4회, 커밋 `bf8f71b`, Ollama 0.34.2, RTX 2080 Ti)

`uv run python -m support_agent.analyze table reports/20260921-011946-qwen2.5-7b-instruct-P0-R0-G0-F0-test-baseline`

| 구성 | pass^1 [95% 구간] | pass^2 | pass^3 | pass^4 | 중단 | infra |
|---|---|---|---|---|---|---|
| 기준 (P0·R0·G0·F0·7B) | 16.2% [8.8, 24.4] | 6.7% | 3.8% | 2.5% | 5 | 0 |

| 유형 | 과제 | pass^1 |
|---|---|---|
| 조회 | 8 | 37.5% [18.8, 56.2] |
| 처리 | 14 | 23.2% [10.7, 39.3] |
| 거절 | 10 | 2.5% [0.0, 7.5] |
| 복합 | 8 | 0.0% |

- **판정: 개선 없음.** 개발용에서 채택한 축이 없어 최종 구성이 없고, 선택 규칙 5에 따라 시험용은 기준만 쟀다. 이 단계에서 시도한 7B용 후보(도구에서 규정 막기, think 도구, 멈춤 가드 둘, 샌 호출 구제)는 성공률을 올리지 못했다
- 시험용 기준은 개발용 기준(20.8%)보다 낮지만 구간이 겹친다. 유형별 모양은 같다: 조회 > 처리 ≫ 거절·복합. 네 번 모두 성공한 과제는 40개 중 1개다 (pass^4 2.5%)
- 정답에 없는 쓰기 19건, 통과된 규정 위반 14건(쿠폰 7, 교환 3, 반품 3, 배송지 1), 본인 확인 전 차단 69건, 형식 오류 73건. 성공한 에피소드 가운데 정답에 없는 쓰기를 한 것은 없다
- 종료 사유: 고객이 끝냄 89, 고객 턴 한도 63, LLM 호출 한도 4, 범위 밖 2, 이관 1, 형식 오류 1. 에피소드당 24.3초
- 시험용 에피소드는 실패 분석에 쓰지 않는다 (개발용으로만 고친다는 규칙). 위 수치는 집계표에서 읽은 것이다

#### 참고: M1 (판정과 무관)
측정 중이다. 끝나면 여기에 적는다.

## 5단계: 음성 채널 (규칙 2026-09-21, 측정 전)

텍스트 채팅으로 만든 상담 에이전트 앞에 음성 인식을 붙이면 성공률이 얼마나 떨어지는가, 규칙 기반 정규화로 얼마나 되찾는가를 잰다. 구현은 `src/support_agent/voice/`, 설명은 [voice.md](voice.md).

### 조건 (기준 구성에서 고객 → 에이전트 방향의 입력만 바꾼다)

| 조건 | 에이전트가 받는 고객 발화 |
|---|---|
| V0 | 시뮬레이터가 쓴 글 그대로 (지금까지의 측정) |
| V1 | 글 → 말로 풀어 쓰기(`verbalize`) → 음성 합성 → 음성 인식 → 인식된 글 |
| V2 | V1의 인식된 글 → 규칙 기반 정규화(`normalize_heard`) |

- 나머지는 2단계의 기준 구성 그대로다 (2단계 판정이 "개선 없음"이었으므로): qwen2.5:7b-instruct, P0·R0·G0·F0, 컨텍스트 16k, 시뮬레이터 모델·프롬프트·temperature 같음
- 음성 합성: MeloTTS 한국어(`KR` 화자, 속도 1.0). 발화마다 seed를 `derive_seed(base_seed, 과제, 시도, "voice", 발화 순번)`으로 고정한다. 이 모델을 고른 근거는 같은 PC에서 잰 재인식 오류다 (한국어 CER 5.5%, 사람 녹음 5.4%. voice-translator 저장소의 `docs/experiments.md`)
- 음성 인식: faster-whisper `large-v3-turbo`, float16, beam 5, temperature 0만(실패 시 온도를 올려 다시 하는 기능을 끔), 언어 `ko`, 이전 문장 조건 없음
- 인식 결과가 비면 에이전트에게 `(고객의 말이 인식되지 않았습니다)`를 준다
- 시뮬레이터는 자기가 쓴 글을 기억한다 (사람은 자기가 한 말을 안다). 에이전트 → 고객 방향은 바꾸지 않는다 (고객은 합성음을 알아듣는다고 가정)
- 같은 (말로 푼 글, seed)의 왕복 결과는 `outputs/voice-cache/`에 남겨 다시 쓴다. 같은 입력이면 같은 결과가 나오는 계산이라 측정값을 바꾸지 않는다
- GPU 메모리에 다 들어가지 않으면 음성 합성만 CPU로 돌린다. 어디서 돌렸는지는 manifest의 `voice_channel`에 남는다

### V0은 다시 재지 않는다
개발용은 `20260920-203406-...-baseline3`, 시험용은 `20260921-011946-...-test-baseline`을 V0으로 쓴다. 조건: 그 manifest의 과제 해시, 프롬프트 해시, seed 해시, Ollama 버전, 모델 digest가 V1 실행의 manifest와 같아야 한다. 하나라도 다르면 V0을 같은 날 다시 잰다. 시험용을 같은 구성으로 두 번 재지 않으려는 것이다.

(2026-09-21 보강, 측정 전. 코드 검토에서 조건이 느슨하다는 지적을 받았다.) 조건은 눈으로 보지 않고 `analyze same-setup <V0 실행> <V1 실행>`으로 확인한다. 이 명령은 `voice`를 뺀 모든 설정 값, 시도 수, 과제·프롬프트·seed 해시, 에이전트와 시뮬레이터 제공자 정보(모델 digest, 서버 버전, 옵션), 과제별로 판정된 시도 번호까지 비교하고 하나라도 다르면 실패한다. 실패하면 V0을 다시 잰다. infra 오류로 판정된 시도 수가 서로 다른 과제가 있어도 실패다.

### V2의 규칙은 개발용 기록만 보고 만든다
1. 개발용 V1을 잰다
2. `analyze voice-worst <개발용 V1 실행>`이 보여 주는 "엔티티를 잃은 발화"만 보고 `voice/normalize.py`를 쓴다. 시험용 과제의 발화와 기록은 보지 않는다 (`voice-worst`는 과제 파일 이름이나 과제 번호가 시험용인 실행을 거부한다. 실수를 막는 장치이고, 일부러 이름을 바꿔 넣는 것까지 막지는 못한다)
3. 정규화 규칙과 그 테스트를 커밋한 뒤 개발용 V2를 잰다

### 지표
- 1차: pass^1. 과제 단위로 짝지은 부트스트랩(10,000회)으로 차이의 95% 구간 (`analyze compare`)
- 2차: 본인 확인에 성공한 에피소드 비율 (`find_customer`가 한 번이라도 성공). 같은 방식의 구간 (`analyze voice`). 모든 과제가 본인 확인에서 시작하고, 기준의 성공률이 낮아(개발용 20.8%, 시험용 16.2%) pass^1만으로는 차이가 안 보일 수 있어 재기 전에 정해 둔다
- 기술용: 글자 오류율(고객이 쓴 글과 에이전트가 받은 글, 공백·문장부호 제외), 엔티티 생존율(전화번호, 이메일, 주문·접수 번호, 금액, 날짜가 도구가 받는 형태 그대로 남았는가), 합성·인식 시간. 이 수치로는 아무것도 판정하지 않는다

### 판정 (시험용 40과제 × 4회)
- V1은 시험용에 한 번 잰다. V2는 개발용에서 pass^1이나 본인 확인 성공률의 점 추정이 V1보다 높을 때만 시험용에 한 번 잰다
- "음성 채널이 성공률을 낮춘다": V1 − V0의 95% 구간이 0보다 아래에 있을 때. "정규화가 되찾는다": V2 − V1의 구간이 0보다 위에 있을 때. 아니면 "차이를 확인하지 못함"
- 같은 문장을 본인 확인 성공률에도 따로 적는다

### 재기 전에 적는 한계
- 합성 음성은 또박또박하고 잡음이 없다. 전화 대역(8 kHz), 주변 소음, 말 더듬, 사투리가 없으므로 실제 통화보다 쉬운 조건이다. 여기서 나오는 하락 폭은 하한에 가깝다
- 말로 풀어 쓰는 규칙이 결과를 좌우한다. "O-91010"을 "오 다시 구일공일공"으로 읽는 것은 여러 읽기 중 하나다
- 시뮬레이터는 음성 통화라는 것을 모른다. 텍스트 채팅처럼 이메일 주소를 불러 주기도 한다. 시뮬레이터를 바꾸면 V0과 비교할 수 없게 되므로 그대로 둔다
- 기준 성공률이 바닥에 가깝다. 떨어질 여지가 작다

### 결과
아직 재지 않았다.
